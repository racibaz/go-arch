all: false
filename: '{{.InterfaceName | snakecase}}_mock.go'
force-file-write: true
formatter: goimports
include-auto-generated: false
log-level: info
recursive: false
require-template-schema-exists: true
template: testify
template-schema: '{{.Template}}.schema.json'
packages:
  #-----------pkg Mocks-----------#

  # Logger interface
  github.com/racibaz/go-arch/pkg/logger:
    config:
      all: true
      # Make use of the template variables to place the mock in the same
      # directory as the original interface.
      dir: "pkg/logger"

  # Messaging interfaces
  github.com/racibaz/go-arch/pkg/messaging:
    config:
      all: true
      # Make use of the template variables to place the mock in the same
      # directory as the original interface.
      dir: "pkg/messaging"

  #-----------Shared Module Mocks-----------#
  # Shared domain interfaces
  github.com/racibaz/go-arch/internal/modules/shared/domain/ports:
    config:
      all: true
      # Make use of the template variables to place the mock in the same
      # directory as the original interface.
      dir: "internal/modules/shared/testing/mocks/domain/ports"


  # Shared domain interfaces
  github.com/racibaz/go-arch/internal/modules/shared/application/ports:
    config:
      all: true
      # Make use of the template variables to place the mock in the same
      # directory as the original interface.
      dir: "internal/modules/shared/testing/mocks/application/ports"


  #-----------Post Module Mocks-----------#
  # Post domain interfaces
  github.com/racibaz/go-arch/internal/modules/post/domain/ports:
    config:
      all: true
      # Make use of the template variables to place the mock in the same
      # directory as the original interface.
      dir: "internal/modules/post/testing/mocks/domain/ports"

  # Post application interfaces
  github.com/racibaz/go-arch/internal/modules/post/features/_shared/ports:
    config:
      all: true
      # Make use of the template variables to place the mock in the same
      # directory as the original interface.
      dir: "internal/modules/post/testing/mocks/features/_shared/ports"


  #-----------User Module Mocks-----------#
  # User domain interfaces
  github.com/racibaz/go-arch/internal/modules/user/domain/ports:
    config:
      all: true
      # Make use of the template variables to place the mock in the same
      # directory as the original interface.
      dir: "internal/modules/user/testing/mocks/domain/ports"

  # Post application interfaces
  github.com/racibaz/go-arch/internal/modules/user/features/_shared/ports:
    config:
      all: true
      # Make use of the template variables to place the mock in the same
      # directory as the original interface.
      dir: "internal/modules/user/testing/mocks/features/_shared/ports"
