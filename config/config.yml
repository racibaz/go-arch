app:
  name: "Hexagonal Architecture, DDD, TDD, RESTful, gRPC, Swagger, Gorm(PostgreSQL) and Gin in Golang"
  debug: "true"
  env: "dev" # local, dev, prod, test
  loglevel: "debug"
  version: "1.0.0"

server:
  host: "0.0.0.0"
  port: "3000"

db:
  host: "postgres" #for docker development
  #host: "localhost" #for local development
  port: "5432"
  testPort: "5433"
  username: "postgres"
  password: "postgres"
  name: "go-arch"

#It is separated from server configs maybe you want to use different host and port for swagger
#in different directory such as cmd/swagger
swagger:
  host: "0.0.0.0"
  port: "8080"
  path: "swagger/doc.json"


grpc:
  host: "0.0.0.0"
  port: "9090"


rabbitmq:
  url: "amqp://guest:guest@rabbitmq:5672/" # for docker development
  #url: "amqp://guest:guest@localhost:5672/" # for local development